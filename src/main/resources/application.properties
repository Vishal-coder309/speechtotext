spring.application.name=speechtotext

#Mongodb Connection
spring.data.mongodb.uri=mongodb+srv://root:root@project.qrihvfs.mongodb.net/myDatabase?retryWrites=true&w=majority&appName=Project

# Server configuration
server.port=8080
server.servlet.context-path=/speech-to-text

# Multipart file configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging configuration
logging.level.root=INFO
logging.level.com.example.speechtotext=DEBUG

# Google Cloud configuration
# Note: Authentication is handled through the GOOGLE_APPLICATION_CREDENTIALS environment variable